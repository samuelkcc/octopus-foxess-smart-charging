{
  "name": "octopus-foxess-smart-charging",
  "version": "2026.7.11",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/test.mjs",
    "check": "node --check src/app.js && npm test && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}
